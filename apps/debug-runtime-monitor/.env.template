# Runtime service metrics.
RUNTIME_BASE_URL=http://localhost:3000
RUNTIME_TOKEN=token
RUNTIME_METRICS_PATH=/api/runtime/metrics
RUNTIME_POLL_INTERVAL_MS=3000

# Connection Gateway metrics.
# When CONNECTION_GATEWAY_BASE_URL is set and RUNTIME_BASE_URL is empty, the monitor reads /metrics.
# CONNECTION_GATEWAY_BASE_URL=http://localhost:3010
# CONNECTION_GATEWAY_AUTH_TOKEN=token

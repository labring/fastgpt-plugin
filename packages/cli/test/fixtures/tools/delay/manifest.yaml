# FastGPT Tool Manifest
# 工具清单配置文件

type: 'tool'

# 工具 ID
toolId: 'delay'

# 标签
tags: []

# 工具名称（多语言支持）
name:
  en: 'delay'
  zh-CN: 'delay'

# 工具描述（多语言支持）
description:
  en: 'delay'
  zh-CN: '延迟执行'

# 工具详细描述（用于 AI 理解工具用途, 可选，如不填则默认获取 description.en）
toolDescription: 'delay'

# 版本号
version: '0.0.1'

# 版本描述（多语言支持）
versionDescription:
  en: 'delay'
  zh-CN: '延迟执行'

# 作者
author: 'FastGPT'
# 图标（可选，若不填则会自动从目录中获取 logo.* 作为图标）
# icon: 'path/to/icon'

# 教程链接（可选）
# tutorialUrl: 'https://example.com/tutorial'

# 子工具列表（可选）
# 如果定义了 children，则此工具为工具集
# 子工具需要在 children/ 目录下定义
# children:
#   - tool1
#   - tool2
